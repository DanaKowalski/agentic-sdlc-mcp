# CI layer: none

No CI configured at setup time.

## Identity

```json
{
  "layer": "ci",
  "id": "none",
  "label": "No CI (add later)"
}
```

## Notes

- Upgrade path: run `/project-setup` again and select a CI layer
- Recommended minimum before first team member joins: `gh-actions-pr`
